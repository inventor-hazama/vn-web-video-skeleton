# Workflow 03: Scale Content (Add Segments)

## Steps
1. Add new clips to `game/movies/` and register in `segments.json` clips map.
2. Add new segments in `segments.json` with pattern/choices.
3. Run web build and verify.

## Outputs
- Content growth remains data-driven (no code edits required for most additions)
